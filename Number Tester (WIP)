#include <stdio.h>
#include <stdbool.h>

int main() {
  double n;
  int operation;

  printf("Type a number:\n");
  fflush(stdout);
  scanf("%lf", &n);

    bool positive_n = n > 0;
    bool negative_n = n < 0;
    bool non_positive_n = n <= 0;
    bool non_negative_n = n >= 0;

  /*
  do
  {
    code
  } while (condition);
  */

  printf("What operation do you want to do?(Type JUST the number):\n1.Sign Checking\n");
  scanf("%d", &operation);

  switch (operation)
  {
  case 1:

    printf("The number n(%lf) posseses these properties:\n"
        "Positive number: %d\n"
        "Negative number: %d\n"
        "Non positive number: %d\n"
        "Non negative number: %d\n", n, positive_n, negative_n, non_positive_n, non_negative_n);

    break;
  case 2:
  default:
  printf("Invalid. Choose a valid option\n");
    break;
  }
}
