#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>




int main(){
    srand(time(NULL));
    int k = 0;
    int n;
    int n_array[10];
    n_array[k] = n;


    for (int i = 0; i < 10; i++) {
    int n = rand() % 1000 + 1;
    
    printf("%d, ", n_array[k]);
    fflush(stdout);
    k++;
}

fflush(stdout);
return 0;
}
