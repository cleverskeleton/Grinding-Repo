#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
    srand(time(NULL));
    int k = 0;
    int n;
    int n_array[10];

    int length = sizeof(n_array) / sizeof(n_array[0]);


    for (int k = 0; k < length; k++) {

    int n = rand() % length + 1;
    n_array[k] = n;
    
    printf("%d, ", n_array[k]);
    fflush(stdout);
}

fflush(stdout);
return 0;
}
